name: tessera_html
description: "HTML export for the tessera pivot-table engine: writes a cube as a table with merged group headers, themed with CubeExportTheme. Pure Dart, no dependencies beyond tessera."
version: 0.1.0
repository: https://github.com/nagylzs/tessera/tree/main/packages/tessera_html
issue_tracker: https://github.com/nagylzs/tessera/issues
topics:
  - pivot-table
  - html
  - export
  - table
resolution: workspace

environment:
  sdk: ^3.13.3

dependencies:
  tessera: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
  xml: ^7.0.1
