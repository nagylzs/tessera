name: tessera
description: "Analyze, group and aggregate tabular data: a pivot-table engine with expandable row/column hierarchies. Pure Dart; tessera_flutter adds the widgets."
version: 0.1.1
repository: https://github.com/nagylzs/tessera/tree/main/packages/tessera
issue_tracker: https://github.com/nagylzs/tessera/issues
topics:
  - pivot-table
  - olap
  - aggregation
  - csv
  - data-analysis
resolution: workspace

environment:
  sdk: ^3.13.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
